# Lifecycle Engine
Canonical project lifecycle: `discover → validate → define → build → verify → release → operate → measure → learn → discover`.

All project transitions must satisfy the applicable gate and state rules.
