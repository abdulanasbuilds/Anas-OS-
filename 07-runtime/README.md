# Runtime

Implementation layer for executing the ANAS OS Kernel and domain workflows locally or in approved environments. Runtime code must consume canonical Kernel definitions rather than maintain shadow policy, lifecycle, or gate registries.
