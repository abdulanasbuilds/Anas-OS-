# Universal Decision Loop

All consequential ANAS OS work follows:

```text
Context → Objective → Constraints → Options → Evidence → Decision → Approval → Execution → Verification → Measurement → Feedback → Learning → System Update
```

The loop is reusable across business, product, engineering, agent execution, operations, and system evolution. Each step records the state needed to inspect why a decision was made and whether it worked.
