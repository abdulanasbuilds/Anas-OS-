# Orchestration
Plans work, decomposes tasks, resolves dependencies, delegates to specialists, coordinates parallel execution, controls concurrency, collects results, and handles partial failure.

Orchestration never changes authority; it invokes Kernel policy, approval, lifecycle, risk, and gate checks before consequential actions.
